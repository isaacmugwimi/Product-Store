{
  "name": "product-store",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development nodemon Backend/server.js",
    "build": "npm install --prefix Frontend && npm run build --prefix Frontend",
    "start": "ls -R && echo '--- Starting Server ---' && ls -R Backend && NODE_ENV=production node Backend/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "mysql2": "^3.14.5",
    "react-toastify": "^11.0.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
